import React from 'react'

export default function Onboarding(){ 
  return <div style={{padding:12}}>
    <h2>Welcome</h2>
    <p>Sign in with your Google account to sync workspace data.</p>
    <p>Changes are saved locally and synced with Google Drive App Data Folder.</p>
    <p><strong>Tip:</strong> Use Workspace → Load Example Data to populate demo content.</p>
  </div>
}